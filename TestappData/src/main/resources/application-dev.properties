# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update): with "create-drop" the database
# schema will be automatically created afresh for every start of application
spring.jpa.hibernate.ddl-auto=update
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.DerbyDialect
# Hibernate ddl auto (create, create-drop, update)
#dataSource.setUrl("jdbc=derby:testDatabase;create=true");
#dataSource.setUsername("");=
#dataSource.setPassword("");=
#spring.datasource.url=jdbc:derby:testDatabase;create=true;user=test;password=test
spring.datasource.username=test
spring.datasource.password=test
spring.datasource.url=jdbc:derby:C:/dev/sourcen/TestApp/testDatabase;create=true